import { Decorator } from "../../src/decorators/types/Decorator.type";
type ExtractArrayType<T> = T extends (infer U)[] ? U : never;
export default function foreach<T extends NonNullable<any[]>>(...validators: Decorator<ExtractArrayType<T>>[]): Decorator<T>;
export {};
