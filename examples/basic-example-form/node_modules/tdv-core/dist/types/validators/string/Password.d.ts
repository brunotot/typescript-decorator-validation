import { DecoratorPartialProps } from "../../src/decorators/types/DecoratorProps.type";
import { $ } from "../../src/types/namespace/Utility.ns";
type PasswordProps = $.Optional<PasswordRequiredProps>;
type PasswordRequiredProps = {
    uppercase: boolean;
    lowercase: boolean;
    numbers: boolean;
    specials: boolean;
    length: number;
};
export default function Password<T extends $.Nullable<string>>(cfg?: DecoratorPartialProps<string, PasswordProps>): import("../../src/decorators/types/Decorator.type").Decorator<T>;
export {};
