import { Class } from "../../types/Class.type";
import { $ } from "../../types/namespace/Utility.ns";
export type PropertyTypeGroup = "PRIMITIVE_ARRAY" | "OBJECT_ARRAY" | "OBJECT" | "PRIMITIVE";
export default class PropertyMetadata<T> {
    #private;
    private _owner;
    private _name;
    private _clazz;
    private _type;
    get parent(): Class<T>;
    get name(): keyof T;
    get clazz(): Class<unknown> | null;
    get type(): PropertyTypeGroup;
    constructor(clazz: Class<T>, field: $.Keys<T>, runtimeValue: unknown);
    private buildTypeGroup;
    private buildClass;
}
