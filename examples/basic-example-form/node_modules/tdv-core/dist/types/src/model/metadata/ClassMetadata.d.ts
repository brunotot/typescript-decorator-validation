import { ValidationGroup } from "../../decorators/types/DecoratorProps.type";
import { Class } from "../../types/Class.type";
import { Payload } from "../../types/Payload.type";
export default class ClassMetadata<T> {
    #private;
    private _clazz;
    private _fieldNames;
    private _validationGroups;
    private _validators;
    constructor(clazz: Class<T>, runtimeValue: T, ...validationGroups: ValidationGroup[]);
    get clazz(): Class<T>;
    get validators(): Payload<T>;
    createInstance(state?: Payload<T>): T;
    private buildValidators;
    private getValidationMetadata;
    private buildFieldNames;
}
