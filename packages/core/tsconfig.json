{
  "extends": "./../../tsconfig.json",
  "exclude": ["tests", "jest.config.js", "src/main.ts", "dev"],
  "files": ["dev/playground.ts"],
  "include": [
    "index.ts",
    "src",
    "src/localization/translations/*.json",
    "translations",
    "collection"
  ],
  "compilerOptions": {
    "module": "ESNext",
    "outDir": "dist",
    "declarationDir": "dist/types",
    "baseUrl": "./", // or the base directory where your index.ts is
    "paths": {
      "api": ["index"]
    }
  }
}
